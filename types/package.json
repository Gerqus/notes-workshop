{
  "name": "types",
  "version": "1.0.2",
  "description": "",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "publish": "npm run build && cd ../frontend && npm i ../types && cd ../backend && npm i ../types",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/mongoose": "^5.7.14"
  }
}
