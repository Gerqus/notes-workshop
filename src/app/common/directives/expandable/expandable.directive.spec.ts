import { ExpandableListDirective } from './expandable-list.directive';

describe('ExpandableListDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandableListDirective();
    expect(directive).toBeTruthy();
  });
});
